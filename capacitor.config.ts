import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tabs_Practica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
